# photobooth
Photobooth app written in Python. (hopefully) Cross platform UI using QT6 library.

## Running
1. Install by running `pip install -r requirements.txt` in root directory
2. Then launch app by running `python3 ./booth_gui.py`